const authControllers = require('./authControllers')
const articleControllers = require('./articleControllers')
const feedControllers = require('./feedControllers')
const webhookControllers = require('./webhookControllers')

module.exports = { authControllers, articleControllers, feedControllers, webhookControllers }
